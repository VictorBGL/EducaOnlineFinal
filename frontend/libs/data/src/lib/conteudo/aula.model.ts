export interface AulaResponseModel {
  id: string;
  titulo: string;
  descricao: string;
  totalHoras: number;
  cursoId: string;
}

export interface AulaModel {
  id: string;
  titulo: string;
  descricao: string;
  totalHoras: number;
  cursoId: string;
}